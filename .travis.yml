language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t petrkoller/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true petrkoller/docker-react npm run test